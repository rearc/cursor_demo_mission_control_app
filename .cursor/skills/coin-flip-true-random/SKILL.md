---
name: coin-flip-true-random
description: Flips a coin using random.org atmospheric noise instead of a pseudorandom PRNG.
---

# Coin Flip (True Random)

Run the script at `.cursor/skills/coin-flip-true-random/scripts/flip.py` using `python3` and report the output as the result. Requires network access to random.org.
